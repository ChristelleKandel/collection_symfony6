{% extends 'base.html.twig' %}

{% block title %}Foo{% endblock %}

{% block body %}
    <h1>Foo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ foo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ foo.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_foo_index') }}">back to list</a>

    <a href="{{ path('app_foo_edit', {'id': foo.id}) }}">edit</a>

    {{ include('foo/_delete_form.html.twig') }}
{% endblock %}
