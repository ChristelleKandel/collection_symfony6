{% extends 'base.html.twig' %}

{% block title %}Baz{% endblock %}

{% block body %}
    <h1>Baz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ baz.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ baz.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_baz_index') }}">back to list</a>

    <a href="{{ path('app_baz_edit', {'id': baz.id}) }}">edit</a>

    {{ include('baz/_delete_form.html.twig') }}
{% endblock %}
